[scripts]
# build_win = "pyinstaller src/__main__.py --windowed --icon assets/icon.ico --add-data 'assets;assets' -n $APP_NAME"
# build_unix = "pyinstaller src/__main__.py --windowed --icon assets/icon.ico --add-data 'assets:assets' -n $APP_NAME"
build = "pyinstaller src/__main__.py --windowed -n $APP_NAME"
dev = "pymon --all src/__main__.py"
start = "python src/__main__.py"
test = "python -m pytest test"
